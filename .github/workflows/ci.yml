name: My first pipeline
on: push
jobs: 
    build:
        runs-on: ubuntu-latest
        steps:
            - name: uptime
              run: uptime
            - name: number of processor
              run: nproc
    test:
        needs: [build]
        runs-on: ubuntu-latest
        steps:
            - name: hello
              run: echo hello 
